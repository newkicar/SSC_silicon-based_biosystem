"""
Marathon Agent — 业务流程马拉松引擎

将复杂HR业务任务拆解为多个子步骤，逐步执行、逐步验证，
实现跨天、跨系统的长流程自动化。

架构：
  外层：LangGraph StateGraph（编排 + 验证 + 持久化）
  内层：大脑 agent loop（deepagents，每步的推理和执行）

核心理念（来自 Claude 7小时马拉松）：
  - 脉冲式循环：每步是一个小循环，不是一次性大推理
  - 外部记忆：进度写入文件和数据库，不依赖对话上下文
  - 双条件门控：模型标记 DONE + 客观验证同时通过才算完成
  - 容错重试：失败后自动重试，超限则升级给人工
"""

__version__ = "0.1.0"